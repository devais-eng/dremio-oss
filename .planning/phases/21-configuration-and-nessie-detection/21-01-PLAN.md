---
phase: 21-configuration-and-nessie-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/icebergcatalog/src/main/java/com/dremio/plugins/icebergcatalog/store/RestIcebergCatalogPluginConfig.java
  - plugins/icebergcatalog/src/main/resources/restcatalog-layout.json
autonomous: true
requirements:
  - CFG-01
  - CMP-01

must_haves:
  truths:
    - "RESTCATALOG source config has an enableNessie boolean field that defaults to false"
    - "enableNessie field persists across restarts via protostuff @Tag(13) serialization"
    - "enableNessie toggle appears in the RESTCATALOG source configuration UI under a Nessie Options section"
    - "Existing RESTCATALOG sources without enableNessie set continue to deserialize correctly with enableNessie=false"
  artifacts:
    - path: "plugins/icebergcatalog/src/main/java/com/dremio/plugins/icebergcatalog/store/RestIcebergCatalogPluginConfig.java"
      provides: "enableNessie config field with @Tag(13)"
      contains: "@Tag(13)"
    - path: "plugins/icebergcatalog/src/main/resources/restcatalog-layout.json"
      provides: "Nessie Options UI section with enableNessie toggle"
      contains: "config.enableNessie"
  key_links:
    - from: "RestIcebergCatalogPluginConfig.java"
      to: "restcatalog-layout.json"
      via: "propName references field name"
      pattern: "config\\.enableNessie"
---

<objective>
Add the `enableNessie` boolean configuration field to the RESTCATALOG source plugin config and expose it in the UI layout.

Purpose: This is the foundational config change for v1.4 -- all subsequent Nessie detection and branch-aware behavior gates on this field. Without it, no new code paths can be activated. The field defaults to `false`, ensuring CMP-01 (zero behavioral change for existing sources).

Output: Modified `RestIcebergCatalogPluginConfig.java` with `@Tag(13) enableNessie` field, updated `restcatalog-layout.json` with Nessie Options section.
</objective>

<execution_context>
@/home/filippo/.claude/get-shit-done/workflows/execute-plan.md
@/home/filippo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-configuration-and-nessie-detection/21-RESEARCH.md
@plugins/icebergcatalog/src/main/java/com/dremio/plugins/icebergcatalog/store/RestIcebergCatalogPluginConfig.java
@plugins/icebergcatalog/src/main/resources/restcatalog-layout.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enableNessie field to RestIcebergCatalogPluginConfig</name>
  <files>plugins/icebergcatalog/src/main/java/com/dremio/plugins/icebergcatalog/store/RestIcebergCatalogPluginConfig.java</files>
  <action>
Add a new boolean field `enableNessie` with `@Tag(13)` and `@DisplayMetadata(label = "Enable Nessie Version Control")` to `RestIcebergCatalogPluginConfig`. The field MUST default to `false`.

Place the field after the existing `@Tag(12)` field (`isRecursiveAllowedNamespaces`). Update the tag range comment at the top of the class to note tag 13 is now used:
```
// 1-9   - IcebergCatalogPluginConfig
// 10-19 - RestIcebergCatalogPluginConfig (10=restEndpointUri, 11=allowedNamespaces, 12=isRecursiveAllowedNamespaces, 13=enableNessie)
// 20-109 - Reserved by other plugins
```

The field declaration should be:
```java
@Tag(13)
@DisplayMetadata(label = "Enable Nessie Version Control")
public boolean enableNessie = false;
```

Do NOT add any behavioral logic in this task -- config classes are pure data holders. Detection logic goes in Plan 02.

IMPORTANT: Use `boolean` (primitive), not `Boolean` (wrapper). Primitive boolean defaults to `false` in Java, and protostuff handles primitive booleans correctly for backward compatibility -- existing serialized configs without this field will deserialize with `enableNessie=false`.
  </action>
  <verify>
Run `grep -n "Tag(13)" plugins/icebergcatalog/src/main/java/com/dremio/plugins/icebergcatalog/store/RestIcebergCatalogPluginConfig.java` confirms the field exists.

Run `grep -n "enableNessie" plugins/icebergcatalog/src/main/java/com/dremio/plugins/icebergcatalog/store/RestIcebergCatalogPluginConfig.java` shows the field with default `false`.

Compile the module: `./mvnw compile -pl plugins/icebergcatalog -am -DskipTests -q` succeeds without errors.
  </verify>
  <done>RestIcebergCatalogPluginConfig has @Tag(13) enableNessie boolean field defaulting to false. Module compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Nessie Options section to restcatalog-layout.json</name>
  <files>plugins/icebergcatalog/src/main/resources/restcatalog-layout.json</files>
  <action>
Add a new "Nessie Options" section to the "General" tab in `restcatalog-layout.json`, placed AFTER the existing "Namespace Filter" section and BEFORE the closing of the "General" tab's sections array.

The new section should be:
```json
{
  "name": "Nessie Options",
  "elements": [
    {
      "propName": "config.enableNessie",
      "validate": { "isRequired": false }
    }
  ]
}
```

This places the Nessie toggle prominently in the General tab where users configure the source, rather than hiding it in Advanced Options. The `propName` MUST match the field name exactly: `config.enableNessie`.

The `validate.isRequired` is `false` because the field has a default value (`false`), so users do not need to explicitly set it.
  </action>
  <verify>
Validate JSON syntax: `python3 -c "import json; json.load(open('plugins/icebergcatalog/src/main/resources/restcatalog-layout.json'))"` succeeds.

Run `grep -c "enableNessie" plugins/icebergcatalog/src/main/resources/restcatalog-layout.json` returns 1.

Run `grep -c "Nessie Options" plugins/icebergcatalog/src/main/resources/restcatalog-layout.json` returns 1.
  </verify>
  <done>restcatalog-layout.json has a "Nessie Options" section in the General tab containing the config.enableNessie toggle. JSON is valid.</done>
</task>

</tasks>

<verification>
1. `RestIcebergCatalogPluginConfig.java` has `@Tag(13) public boolean enableNessie = false`
2. `restcatalog-layout.json` has `"Nessie Options"` section with `"config.enableNessie"` element
3. `./mvnw compile -pl plugins/icebergcatalog -am -DskipTests -q` compiles without errors
4. No other files in the module were modified (CMP-01: zero behavioral change)
</verification>

<success_criteria>
- enableNessie field exists with @Tag(13), defaults to false, compiles cleanly
- UI layout JSON is valid and contains the Nessie Options section
- No behavioral code was added -- this is config-only
</success_criteria>

<output>
After completion, create `.planning/phases/21-configuration-and-nessie-detection/21-01-SUMMARY.md`
</output>
