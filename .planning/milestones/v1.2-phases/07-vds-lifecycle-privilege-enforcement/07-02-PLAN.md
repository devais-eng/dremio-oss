---
phase: 07-vds-lifecycle-privilege-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java
  - sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/CreateOrUpdateViewHandler.java
  - dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03

must_haves:
  truths:
    - "DROP VIEW via SQL DDL checks DROP privilege (not ALTER) on the view"
    - "CREATE OR REPLACE VIEW via SQL DDL checks ALTER privilege when the view already exists (update path)"
    - "CREATE VIEW via SQL DDL checks CREATE_VIEW privilege on the parent container (container-scoped)"
    - "CREATE VIEW via REST API checks CREATE_VIEW privilege on the parent container before calling viewCreatorFactory"
    - "UPDATE VIEW via REST API (versioned and non-versioned) checks ALTER privilege before calling catalog.updateView()"
    - "DELETE VDS via REST API (non-versioned) checks DROP privilege before calling namespaceService.deleteDataset()"
    - "DELETE versioned view via REST API checks DROP privilege before calling catalog.dropView()"
  artifacts:
    - path: "sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java"
      provides: "DROP privilege enforcement on SQL DROP VIEW"
      contains: "SqlGrant.Privilege.DROP"
    - path: "sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/CreateOrUpdateViewHandler.java"
      provides: "ALTER enforcement on update path, container-scoped CREATE_VIEW on create path"
      contains: "validateCreateViewPrivilege"
    - path: "dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java"
      provides: "CREATE_VIEW, ALTER, and DROP enforcement on REST API paths"
      contains: "validateCreateViewPrivilege"
  key_links:
    - from: "DropViewHandler.toResult()"
      to: "catalog.validatePrivilege(path, SqlGrant.Privilege.DROP)"
      via: "direct call before drop operation"
      pattern: "Privilege\\.DROP"
    - from: "CreateOrUpdateViewHandler.createView()/createVersionedView()"
      to: "catalog.validatePrivilege(viewPath, SqlGrant.Privilege.ALTER)"
      via: "conditional call when isUpdate is true"
      pattern: "isUpdate.*validatePrivilege.*ALTER"
    - from: "CreateOrUpdateViewHandler.toResult()"
      to: "catalog.validateCreateViewPrivilege(resolvedViewPath)"
      via: "container-scoped call replacing old validatePrivilege(CREATE_VIEW)"
      pattern: "validateCreateViewPrivilege"
    - from: "CatalogServiceHelper.createDataset()"
      to: "catalogSupplier.get().validateCreateViewPrivilege(namespaceKey)"
      via: "call before viewCreatorFactory.createView()"
      pattern: "validateCreateViewPrivilege"
    - from: "CatalogServiceHelper.deleteDataset() VIRTUAL_DATASET case"
      to: "catalogSupplier.get().validatePrivilege(namespaceKey, SqlGrant.Privilege.DROP)"
      via: "call before namespaceService.deleteDataset()"
      pattern: "Privilege\\.DROP"
---

<objective>
Wire ALTER, DROP, and CREATE_VIEW enforcement into all SQL DDL and REST API call sites. Fix the DropViewHandler bug (ALTER -> DROP), add ALTER checks for update paths in CreateOrUpdateViewHandler, switch CreateOrUpdateViewHandler to use the container-scoped validateCreateViewPrivilege(), and add privilege checks to all CatalogServiceHelper VDS mutation paths (create, update, delete).

Purpose: Closes every enforcement gap so no VDS lifecycle operation succeeds without an explicit grant. Without this, the infrastructure from Plan 01 exists but is not called at the right points.

Output: All SQL DDL and REST API paths for VDS create/alter/drop are privilege-gated.
</objective>

<execution_context>
@/home/filippo/.claude/get-shit-done/workflows/execute-plan.md
@/home/filippo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-vds-lifecycle-privilege-enforcement/07-RESEARCH.md
@.planning/phases/07-vds-lifecycle-privilege-enforcement/07-01-SUMMARY.md
@sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java
@sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/CreateOrUpdateViewHandler.java
@dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DropViewHandler and CreateOrUpdateViewHandler privilege enforcement</name>
  <files>
    sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java
    sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/CreateOrUpdateViewHandler.java
  </files>
  <action>
    **DropViewHandler.java — Fix privilege bug (LIFE-02):**

    At line 55 in `toResult()`, change:
    ```java
    catalog.validatePrivilege(path, SqlGrant.Privilege.ALTER);
    ```
    To:
    ```java
    catalog.validatePrivilege(path, SqlGrant.Privilege.DROP);
    ```
    This is the only change needed in DropViewHandler. The rest of the method is correct.

    **CreateOrUpdateViewHandler.java — Three changes (LIFE-01, LIFE-03):**

    1. **Switch toResult() CREATE_VIEW call to container-scoped method** (line 105):
       Change:
       ```java
       catalog.validatePrivilege(resolvedViewPath, SqlGrant.Privilege.CREATE_VIEW);
       ```
       To:
       ```java
       catalog.validateCreateViewPrivilege(resolvedViewPath);
       ```
       This switches from checking CREATE_VIEW on the view path to checking CREATE_VIEW on the parent container path. The `validateCreateViewPrivilege()` method (from Plan 01) internally calls `viewKey.getParent().getSchemaPath()`.

    2. **Add ALTER check in createVersionedView() for update path** (after line 147 where `isUpdate &= exists;`):
       After `isUpdate &= exists;` and before `final ViewOptions viewOptions = ...`, add:
       ```java
       if (isUpdate) {
           catalog.validatePrivilege(viewPath, SqlGrant.Privilege.ALTER);
       }
       ```
       This ensures CREATE OR REPLACE VIEW on a versioned source requires ALTER when the view already exists.

    3. **Add ALTER check in createView() (non-versioned) for update path** (after line 174 where `isUpdate &= exists;`):
       After `isUpdate &= exists;` and before `final View view = ...`, add:
       ```java
       if (isUpdate) {
           catalog.validatePrivilege(viewPath, SqlGrant.Privilege.ALTER);
       }
       ```
       This ensures CREATE OR REPLACE VIEW on a non-versioned source requires ALTER when the view already exists.

    Note: The `catalog` field is available in both private methods — it is an instance field set in the constructor. The `SqlGrant` import already exists in both files.
  </action>
  <verify>
    Grep DropViewHandler.java for "Privilege.DROP" — should find exactly 1 match at line 55.
    Grep DropViewHandler.java for "Privilege.ALTER" — should find 0 matches (bug is fixed).
    Grep CreateOrUpdateViewHandler.java for "validateCreateViewPrivilege" — should find 1 match in toResult().
    Grep CreateOrUpdateViewHandler.java for "Privilege.ALTER" — should find 2 matches (one in each private create method).
    Grep CreateOrUpdateViewHandler.java for "Privilege.CREATE_VIEW" — should find 0 matches (replaced by container-scoped call).
  </verify>
  <done>
    DropViewHandler.toResult() validates DROP (not ALTER) on the view path.
    CreateOrUpdateViewHandler.toResult() validates CREATE_VIEW via container-scoped method.
    CreateOrUpdateViewHandler.createVersionedView() validates ALTER when isUpdate is true.
    CreateOrUpdateViewHandler.createView() (non-versioned) validates ALTER when isUpdate is true.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add privilege enforcement to CatalogServiceHelper REST API paths for VDS create, update, and delete</name>
  <files>
    dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
  </files>
  <action>
    Add `validatePrivilege` / `validateCreateViewPrivilege` calls to all REST API paths that mutate VDS entities. The `catalogSupplier.get()` returns a `Catalog` (which extends `DatasetCatalog` where both methods are declared). The `SqlGrant` import must be added if not already present.

    **Add import** (if not already present):
    ```java
    import com.dremio.exec.planner.sql.parser.SqlGrant;
    ```

    **1. createDataset() — CREATE_VIEW enforcement (LIFE-03):**
    In `createDataset()` method (around line 1292), add the privilege check BEFORE the `sabotContext.getViewCreatorFactoryProvider()...createView(...)` call. After `Preconditions.checkArgument(rootEntity != null, ...)` at line 1291 and before line 1293:
    ```java
    // RBAC: enforce CREATE_VIEW privilege on parent container
    catalogSupplier.get().validateCreateViewPrivilege(namespaceKey);
    ```

    **2. updateVersionedDataset() — ALTER enforcement (LIFE-01):**
    In the `updateVersionedDataset()` method, add ALTER check before the `catalogSupplier.get().updateView(...)` call at line 1470. After the `viewOptions` construction at line 1468 and before line 1470:
    ```java
    // RBAC: enforce ALTER privilege on the view being updated
    catalogSupplier.get().validatePrivilege(namespaceKey, SqlGrant.Privilege.ALTER);
    ```

    **3. updateNonVersionedDataset() — ALTER enforcement (LIFE-01):**
    In `updateNonVersionedDataset()`, the VDS update path leads to `catalogSupplier.get().updateView(...)` at line 1693. However, this method handles both PDS and VDS updates. The VDS update path is inside the else branch starting around line 1660 where `type == VIRTUAL_DATASET`. Add the ALTER check BEFORE the `catalogSupplier.get().updateView(...)` call at line 1691. Read the exact code path carefully to place the check right before the updateView call, only in the VIRTUAL_DATASET branch:
    ```java
    // RBAC: enforce ALTER privilege on the view being updated
    catalogSupplier.get().validatePrivilege(namespaceKey, SqlGrant.Privilege.ALTER);
    ```

    **4. deleteDataset() VIRTUAL_DATASET case — DROP enforcement (LIFE-02):**
    In `deleteDataset()` (line 1701), inside the `case VIRTUAL_DATASET:` block (line 1731), add DROP check BEFORE the `namespaceService.deleteDataset(...)` call at line 1733:
    ```java
    case VIRTUAL_DATASET:
    {
        // RBAC: enforce DROP privilege on the view being deleted
        catalogSupplier.get().validatePrivilege(new NamespaceKey(config.getFullPathList()), SqlGrant.Privilege.DROP);
        namespaceService.deleteDataset(new NamespaceKey(config.getFullPathList()), version);
        break;
    }
    ```

    **5. deleteVersionedView() — DROP enforcement (LIFE-02):**
    In `deleteVersionedView()` (line 2041), add DROP check BEFORE the `catalogSupplier.get().dropView(...)` call at line 2048:
    ```java
    // RBAC: enforce DROP privilege on the versioned view being deleted
    catalogSupplier.get().validatePrivilege(namespaceKey, SqlGrant.Privilege.DROP);
    ```

    Important: Do NOT add privilege checks to `deleteVersionedTable()` (line 2054) or `deleteVersionedFunction()` (line 2061) — those are table and function paths, not VDS paths. Phase 7 scope is VDS lifecycle only.
  </action>
  <verify>
    Grep CatalogServiceHelper.java for "validateCreateViewPrivilege" — should find exactly 1 match (in createDataset).
    Grep CatalogServiceHelper.java for "Privilege.ALTER" — should find exactly 2 matches (updateVersionedDataset, updateNonVersionedDataset).
    Grep CatalogServiceHelper.java for "Privilege.DROP" — should find exactly 2 matches (deleteDataset VIRTUAL_DATASET case, deleteVersionedView).
    Grep CatalogServiceHelper.java for "SqlGrant" — should find import + usage.
    Confirm no privilege checks added to deleteVersionedTable or deleteVersionedFunction.
  </verify>
  <done>
    REST API VDS creation path (createDataset) enforces CREATE_VIEW on parent container.
    REST API VDS update paths (updateVersionedDataset, updateNonVersionedDataset) enforce ALTER on the view.
    REST API VDS delete paths (deleteDataset VIRTUAL_DATASET, deleteVersionedView) enforce DROP on the view.
    PDS and function paths are untouched (out of scope for Phase 7).
  </done>
</task>

</tasks>

<verification>
1. Grep DropViewHandler.java for "Privilege.DROP" — exactly 1 match
2. Grep DropViewHandler.java for "Privilege.ALTER" — 0 matches (bug fixed)
3. Grep CreateOrUpdateViewHandler.java for "validateCreateViewPrivilege" — 1 match (container-scoped CREATE_VIEW)
4. Grep CreateOrUpdateViewHandler.java for "Privilege.ALTER" — 2 matches (versioned + non-versioned update paths)
5. Grep CreateOrUpdateViewHandler.java for "Privilege.CREATE_VIEW" — 0 matches (replaced by container-scoped call)
6. Grep CatalogServiceHelper.java for "validateCreateViewPrivilege" — 1 match (createDataset)
7. Grep CatalogServiceHelper.java for "Privilege.ALTER" — 2 matches (versioned + non-versioned update)
8. Grep CatalogServiceHelper.java for "Privilege.DROP" — 2 matches (non-versioned + versioned delete)
9. Count total enforcement call sites across all 3 files: 8 (1 DropViewHandler + 3 CreateOrUpdateViewHandler + 4 CatalogServiceHelper = 8, but toResult CREATE_VIEW is 1, versioned ALTER 1, non-versioned ALTER 1, REST create 1, REST update versioned 1, REST update non-versioned 1, REST delete non-versioned 1, REST delete versioned 1 = 8 enforcement points)
</verification>

<success_criteria>
- DropViewHandler validates DROP privilege (not ALTER) — LIFE-02 via SQL DDL
- CreateOrUpdateViewHandler validates CREATE_VIEW on parent container — LIFE-03 via SQL DDL
- CreateOrUpdateViewHandler validates ALTER on both versioned and non-versioned update paths — LIFE-01 via SQL DDL
- CatalogServiceHelper.createDataset() validates CREATE_VIEW on parent container — LIFE-03 via REST API
- CatalogServiceHelper update paths validate ALTER — LIFE-01 via REST API
- CatalogServiceHelper delete paths validate DROP — LIFE-02 via REST API
- Admin bypass works for all operations (inherited from validatePrivilege/validateCreateViewPrivilege implementation)
- GRANT/REVOKE ALTER and DROP work via existing CatalogGrantHandler/CatalogRevokeHandler (no changes needed — confirmed by research)
</success_criteria>

<output>
After completion, create `.planning/phases/07-vds-lifecycle-privilege-enforcement/07-02-SUMMARY.md`
</output>
