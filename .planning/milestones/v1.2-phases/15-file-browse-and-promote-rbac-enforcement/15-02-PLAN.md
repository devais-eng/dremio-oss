---
phase: 15-file-browse-and-promote-rbac-enforcement
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java
  - dac/backend/src/test/java/com/dremio/dac/resource/TestSourceResourceRbac.java
autonomous: true
requirements: [FILE-01, FILE-02]

must_haves:
  truths:
    - "Each guard has three test cases: RBAC disabled (no block), RBAC enabled + admin (no block), RBAC enabled + non-admin (throws Permission denied)"
    - "SourceResource browse tests cover getSource, getFolder, and getFile"
    - "SourceResource promote tests cover saveFormatSettings and saveFolderFormat"
    - "CatalogServiceHelper browse tests cover getCatalogEntityFromNonPromotedFileOrFolder and getCatalogEntityFromCatalogItem (indirectly via getCatalogEntityByPath and getInternalObjectById)"
    - "CatalogServiceHelper promote test covers promoteToDataset"
  artifacts:
    - path: "dac/backend/src/test/java/com/dremio/dac/resource/TestSourceResourceRbac.java"
      provides: "Unit tests for SourceResource RBAC guards"
      min_lines: 100
    - path: "dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java"
      provides: "Unit tests for CatalogServiceHelper browse and promote guards"
      contains: "testPromoteToDataset_nonAdmin"
  key_links:
    - from: "TestSourceResourceRbac.java"
      to: "SourceResource"
      via: "direct construction with mocked dependencies"
      pattern: "new SourceResource\\("
    - from: "TestCatalogServiceHelper.java"
      to: "rbacEnabledHelper"
      via: "existing test fixture"
      pattern: "rbacEnabledHelper"
---

<objective>
Add unit tests for all 8 RBAC admin guards introduced in Plan 01, covering the three-case test matrix (RBAC disabled, admin, non-admin) for each guarded method.

Purpose: Verify that file browse and promote restrictions work correctly for all user/config combinations, preventing regressions.
Output: New TestSourceResourceRbac.java (SourceResource guards) and updated TestCatalogServiceHelper.java (CatalogServiceHelper guards).
</objective>

<execution_context>
@/home/filippo/.claude/get-shit-done/workflows/execute-plan.md
@/home/filippo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-file-browse-and-promote-rbac-enforcement/15-RESEARCH.md
@.planning/phases/15-file-browse-and-promote-rbac-enforcement/15-01-SUMMARY.md
@dac/backend/src/main/java/com/dremio/dac/resource/SourceResource.java
@dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
@dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestSourceResourceRbac unit test class</name>
  <files>dac/backend/src/test/java/com/dremio/dac/resource/TestSourceResourceRbac.java</files>
  <action>
Create a new unit test class for SourceResource RBAC guards using Mockito mocks (NOT BaseTestServer integration tests). The test constructs SourceResource directly with mocked dependencies.

**Test fixture setup:**
- Mock all SourceResource constructor dependencies: NamespaceService, ReflectionAdministrationService.Factory, SourceService, SourceName, QueryExecutor, SecurityContext, ConnectionReader, SourceCatalog, FormatTools, BufferAllocatorFactory, OptionManager, Provider of Orphanage.Factory, CatalogService
- Mock RbacService and DremioConfig (the new RBAC params)
- Mock SecurityContext to return a Principal with getName() = "testuser"
- Mock NamespaceService.getSource() to return a SourceConfig (prevents SourceNotFoundException in constructor)
- Mock CatalogService.getSourceState() to return a non-null SourceState (prevents NPE in getSource())
- Three helper methods to create SourceResource instances:
  - `createResourceRbacDisabled()` — passes null for rbacService and dremioConfig
  - `createResourceRbacAdmin()` — passes mocked rbacService/dremioConfig, isAdminMember returns true
  - `createResourceRbacNonAdmin()` — passes mocked rbacService/dremioConfig, isAdminMember returns false

**FILE-01 browse tests (9 tests = 3 methods x 3 cases):**
- `testGetSource_rbacDisabled_allowed()` — rbacService=null, no exception thrown (method may throw other exceptions from missing mocks, that's OK as long as it's not UserException with "Permission denied")
- `testGetSource_admin_allowed()` — rbacEnabled + admin, no Permission denied
- `testGetSource_nonAdmin_blocked()` — rbacEnabled + non-admin, assertThatThrownBy isInstanceOf(UserException.class).hasMessageContaining("Permission denied").hasMessageContaining("browse source files")
- Same 3 tests for `getFolder()` (call with path="test/folder", any params)
- Same 3 tests for `getFile()` (call with path="test/file.csv")

For the "allowed" test cases: The method may throw other exceptions (SourceNotFoundException, NamespaceException, etc.) because downstream mocks are not fully wired. That is acceptable. The test only needs to verify that the RBAC guard does NOT throw. Use a try-catch pattern: call the method, catch any exception, and assert the exception is NOT a UserException with "Permission denied". OR use `assertThatThrownBy(...).isNotInstanceOf(UserException.class)` if the call does throw, or simply verify `rbacService.isAdminMember` was called (for admin case) or never called (for disabled case).

**Recommended simpler approach for "allowed" tests:** Since the guard is the FIRST thing in each method, if the guard passes, the method will proceed to downstream calls that will likely throw (due to incomplete mocking). Verify the guard behavior by checking that `rbacService.isAdminMember()` was called (admin case) or not called (disabled case), using Mockito verify(). For the non-admin blocked case, use assertThatThrownBy.

**FILE-02 promote tests (6 tests = 2 methods x 3 cases):**
- Same pattern for `saveFormatSettings()` — call with a mock FileFormat and path="test/file.csv"
- Same pattern for `saveFolderFormat()` — call with a mock FileFormat and path="test/folder"
- Non-admin tests: assertThatThrownBy hasMessageContaining("Permission denied").hasMessageContaining("promote")

Use JUnit 4 (@Test annotation) to match the existing TestSourceResource.java pattern in the same package. Use AssertJ assertions (assertThat, assertThatThrownBy) consistent with the codebase.
  </action>
  <verify>
`grep -c "@Test" dac/backend/src/test/java/com/dremio/dac/resource/TestSourceResourceRbac.java` should show at least 15 (5 non-admin blocked + 5 admin allowed + 5 rbac-disabled allowed). All tests should be compilable (check imports resolve).
  </verify>
  <done>TestSourceResourceRbac.java exists with at least 15 unit tests covering the three-case matrix for all 5 SourceResource RBAC guards (getSource, getFolder, getFile, saveFormatSettings, saveFolderFormat).</done>
</task>

<task type="auto">
  <name>Task 2: Add CatalogServiceHelper browse and promote guard tests</name>
  <files>dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java</files>
  <action>
Add unit tests to the existing TestCatalogServiceHelper.java for the 3 new CatalogServiceHelper guards. Use the existing `rbacEnabledHelper` fixture (lines 171, 244-256) which already has RbacService and DremioConfig mocked with RBAC_ENABLED=true.

**FILE-02 promote guard test (3 tests):**

1. `testPromoteToDataset_nonAdmin_getsPermissionDenied()`:
   - `when(rbacService.isAdminMember("user")).thenReturn(false);`
   - Create a Dataset with type PHYSICAL_DATASET and a target ID
   - `assertThatThrownBy(() -> rbacEnabledHelper.promoteToDataset("internalId", dataset)).isInstanceOf(UserException.class).hasMessageContaining("Permission denied").hasMessageContaining("promote")`
   - Verify promoteToDataset guard fires before Preconditions.checkArgument

2. `testPromoteToDataset_admin_noBlock()`:
   - `when(rbacService.isAdminMember("user")).thenReturn(true);`
   - Call promoteToDataset — it may throw other exceptions from missing mocks, but NOT UserException with "Permission denied"
   - Verify isAdminMember was called

3. `testPromoteToDataset_rbacDisabled_noBlock()`:
   - Use the regular (non-RBAC) helper instance, or create a helper with rbacService=null
   - Call promoteToDataset — verify no Permission denied (may throw other exceptions)

**FILE-01 browse guard tests (6 tests for 2 methods x 3 cases):**

The browse methods (getCatalogEntityFromNonPromotedFileOrFolder and getCatalogEntityFromCatalogItem) are private. Test them indirectly:

4. `testBrowseNonPromotedFileOrFolder_nonAdmin_getsPermissionDenied()`:
   - Exercise via `rbacEnabledHelper.getCatalogEntityByPath(path, null, null, null, null, false)` with a path that resolves to a non-promoted file/folder
   - Setup: `when(rbacService.isAdminMember("user")).thenReturn(false);`
   - Mock namespaceService so getEntityByPath returns null (entity not in namespace, triggers getCatalogEntityFromNonPromotedFileOrFolder path)
   - assertThatThrownBy isInstanceOf(UserException.class).hasMessageContaining("Permission denied").hasMessageContaining("browse source files")

5. `testBrowseNonPromotedFileOrFolder_admin_noBlock()`:
   - Same setup but isAdminMember returns true
   - Guard passes; method continues (may throw other exceptions from missing mocks)

6. `testBrowseNonPromotedFileOrFolder_rbacDisabled_noBlock()`:
   - Use non-RBAC helper or null rbacService helper
   - Guard is no-op

7-9. For getCatalogEntityFromCatalogItem via the by-id path: These tests are harder to set up because the by-id path goes through getInternalObjectById. If setup is too complex, document that the guard is tested via the by-path test (both guards use the same pattern and same guard block). At minimum, add a comment explaining coverage.

Place all new tests at the end of the file, before the closing brace, grouped under a comment:
```java
// --- FILE-01/FILE-02: File browse and promote RBAC enforcement tests (Phase 15) ---
```

Use the same assertion style as the existing `testCreateSource_nonAdmin_getsPermissionDenied()` test (assertThatThrownBy + isInstanceOf + hasMessageContaining).
  </action>
  <verify>
`grep -c "FILE-01\|FILE-02\|testPromoteToDataset\|testBrowseNonPromoted" dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java` should show at least 6 new test-related lines. `grep -c "@Test" dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java` should show count increased by at least 6 compared to before.
  </verify>
  <done>TestCatalogServiceHelper.java has at least 6 new tests covering the three-case matrix for CatalogServiceHelper promoteToDataset guard and getCatalogEntityFromNonPromotedFileOrFolder guard (indirectly via getCatalogEntityByPath).</done>
</task>

</tasks>

<verification>
1. Total new test methods: at least 15 (SourceResource) + 6 (CatalogServiceHelper) = 21 tests
2. Each guarded method has at least: 1 test for non-admin blocked, 1 test for admin allowed, 1 test for RBAC disabled
3. All non-admin tests assert UserException with "Permission denied" message
4. All admin/disabled tests verify the guard does not throw Permission denied
</verification>

<success_criteria>
- TestSourceResourceRbac.java exists with tests for all 5 SourceResource guard methods
- TestCatalogServiceHelper.java has new tests for promoteToDataset and browse guards
- Every test follows the 3-case pattern: RBAC disabled, admin, non-admin
- Non-admin tests use assertThatThrownBy with Permission denied assertion
</success_criteria>

<output>
After completion, create `.planning/phases/15-file-browse-and-promote-rbac-enforcement/15-02-SUMMARY.md`
</output>
