---
phase: 13-code-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sabot/kernel/src/main/java/com/dremio/exec/catalog/CatalogImpl.java
  - sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java
  - dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
  - sabot/kernel/src/test/java/com/dremio/exec/catalog/TestCatalogImpl.java
  - sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/TestDropViewHandler.java
  - dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java
autonomous: true
requirements:
  - PDS-02
  - LIFE-02

must_haves:
  truths:
    - "getTable(String datasetId) calls isRbacDeniedForPds on the resolved table, closing INT-01"
    - "A user with DROP but not SELECT on a VDS gets 'Permission denied: SELECT privilege required' instead of 'Unknown view'"
    - "A non-admin user calling createSource() for an existing source gets 'Permission denied' instead of 'already exists'"
  artifacts:
    - path: "sabot/kernel/src/main/java/com/dremio/exec/catalog/CatalogImpl.java"
      provides: "PDS enforcement in getTable(String datasetId)"
      contains: "isRbacDeniedForPds(table, table.getPath())"
    - path: "sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java"
      provides: "SELECT privilege check before view resolution in DROP VIEW"
      contains: "validatePrivilege(path, SqlGrant.Privilege.SELECT)"
    - path: "dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java"
      provides: "Admin-only guard in createSource before delegation to sourceService"
      contains: "rbacService.isAdminMember"
    - path: "sabot/kernel/src/test/java/com/dremio/exec/catalog/TestCatalogImpl.java"
      provides: "Unit test documenting getTable(String) PDS enforcement contract"
      contains: "testGetTableByDatasetId_pdsEnforcement"
    - path: "sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/TestDropViewHandler.java"
      provides: "Unit test for DROP VIEW SELECT-required UX fix"
      contains: "testDropView_dropOnlyUser_getsPermissionDenied"
    - path: "dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java"
      provides: "Unit test for createSource admin-only enforcement"
      contains: "testCreateSource_nonAdmin_getsPermissionDenied"
  key_links:
    - from: "CatalogImpl.getTable(String)"
      to: "isRbacDeniedForPds()"
      via: "inline call after table resolution"
      pattern: "isRbacDeniedForPds\\(table, table\\.getPath\\(\\)\\)"
    - from: "DropViewHandler.toResult()"
      to: "catalog.validatePrivilege(SELECT)"
      via: "explicit check after DROP check"
      pattern: "validatePrivilege\\(path, SqlGrant\\.Privilege\\.SELECT\\)"
    - from: "CatalogServiceHelper.createSource()"
      to: "rbacService.isAdminMember()"
      via: "guard before sourceService.createSource()"
      pattern: "rbacService\\.isAdminMember\\(userName\\)"
---

<objective>
Close three code-level gaps identified by the v1.2 audit: (1) add PDS enforcement to
CatalogImpl.getTable(String datasetId) closing INT-01, (2) add explicit SELECT check in
DropViewHandler so DROP-only users see "Permission denied" not "Unknown view", (3) add
admin guard in CatalogServiceHelper.createSource() so non-admins see "Permission denied"
not "already exists".

Purpose: Harden v1.2 RBAC enforcement by closing the last three code-level gaps before the
final test/docs phase.
Output: Three production code fixes + three unit tests documenting new contracts.
</objective>

<execution_context>
@/home/filippo/.claude/get-shit-done/workflows/execute-plan.md
@/home/filippo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-code-hardening/13-RESEARCH.md
@sabot/kernel/src/main/java/com/dremio/exec/catalog/CatalogImpl.java
@sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java
@dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
@sabot/kernel/src/test/java/com/dremio/exec/catalog/TestCatalogImpl.java
@sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/TestDescribeTableHandler.java
@dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply three surgical production code fixes</name>
  <files>
    sabot/kernel/src/main/java/com/dremio/exec/catalog/CatalogImpl.java
    sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java
    dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java
  </files>
  <action>
    **Fix 1 -- CatalogImpl.getTable(String datasetId) PDS enforcement (INT-01 / PDS-02):**

    In CatalogImpl.java, locate `getTable(String datasetId)` at line ~1213. Replace the current
    method body with:

    1. Remove the TODO comment at line 1214-1215.
    2. Extract the resolved table into a local variable `final DremioTable table;`.
    3. Use an if/else to assign: `table = getTableForTimeTravel(versionedDatasetId)` or
       `table = datasetManager.getTable(datasetId, options)`.
    4. After the if/else, add the PDS enforcement guard:
       `if (table != null && isRbacDeniedForPds(table, table.getPath())) { return null; }`
    5. Return the table.

    The result should match the "Fixed version" in 13-RESEARCH.md Pattern 1. Key constraints:
    - Use `table.getPath()` (returns NamespaceKey), NOT `new NamespaceKey(datasetId)` -- datasetId
      is a JSON blob, not a path.
    - Null guard (`table != null`) prevents NPE when dataset doesn't exist.
    - `isRbacDeniedForPds` is safe for VDS (returns false for ViewTable instances).

    **Fix 2 -- DropViewHandler.toResult() SELECT check (LIFE-02 UX):**

    In DropViewHandler.java, locate `toResult()` at line ~52. Immediately after the existing
    `catalog.validatePrivilege(path, SqlGrant.Privilege.DROP);` at line 55, add:

    ```java
    // LIFE-02: SELECT is required to resolve the view before dropping it.
    // Without this check, DROP-only users get "Unknown view" instead of "Permission denied".
    catalog.validatePrivilege(path, SqlGrant.Privilege.SELECT);
    ```

    The SELECT check MUST come BEFORE `getTableNoColumnCount()` (line 71-73), NOT after.
    `validatePrivilege()` already has an internal RBAC_ENABLED guard, so no outer gate is needed.

    **Fix 3 -- CatalogServiceHelper.createSource() admin guard (Finding 4):**

    In CatalogServiceHelper.java, locate `createSource()` at line ~1850. Add an admin check
    at the top of the method body, before `sourceService.createSource()`:

    ```java
    // Phase 13 Finding 4: enforce admin-only before existence check to prevent metadata leak.
    // Non-admin users must see "Permission denied" not "Source already exists".
    if (rbacService != null
        && dremioConfig != null
        && dremioConfig.getBoolean(DremioConfig.RBAC_ENABLED)) {
      String userName = securityContext.getUserPrincipal().getName();
      if (!rbacService.isAdminMember(userName)) {
        throw UserException.validationError()
            .message("Permission denied: only administrators can create sources.")
            .buildSilently();
      }
    }
    ```

    Key constraints:
    - Guard on `rbacService != null && dremioConfig != null` -- both are @Nullable.
    - Guard on `RBAC_ENABLED` -- when RBAC is off, @RolesAllowed JAX-RS annotation handles auth.
    - Use `UserException.validationError()` -- matches existing error patterns in this file.
    - This check goes BEFORE `sourceService.createSource()` to prevent the "already exists" leak.
  </action>
  <verify>
    1. `grep -n "isRbacDeniedForPds(table, table.getPath())" sabot/kernel/src/main/java/com/dremio/exec/catalog/CatalogImpl.java` matches inside getTable(String datasetId).
    2. `grep -n "TODO.*PDS enforcement" sabot/kernel/src/main/java/com/dremio/exec/catalog/CatalogImpl.java` returns no matches (TODO removed).
    3. `grep -n "validatePrivilege.*SELECT" sabot/kernel/src/main/java/com/dremio/exec/planner/sql/handlers/direct/DropViewHandler.java` shows the new SELECT check.
    4. `grep -n "isAdminMember" dac/backend/src/main/java/com/dremio/dac/service/catalog/CatalogServiceHelper.java` includes the createSource guard.
  </verify>
  <done>
    All three production code gaps are closed: getTable(String) enforces PDS RBAC, DropViewHandler
    checks SELECT before view resolution, CatalogServiceHelper.createSource() checks admin before
    existence. No TODO comments remain for these gaps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for all three code fixes</name>
  <files>
    sabot/kernel/src/test/java/com/dremio/exec/catalog/TestCatalogImpl.java
    sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/TestDropViewHandler.java
    dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java
  </files>
  <action>
    **Test 1 -- TestCatalogImpl: getTable(String datasetId) PDS enforcement contract:**

    Add a test method `testGetTableByDatasetId_pdsEnforcement` near the existing PDS tests
    (around line ~2144). This test documents that `getTable(String datasetId)` now participates
    in PDS enforcement. Since `datasetManager.getTable(datasetId, options)` returns null for
    non-existent IDs in the unit test environment (DatasetManager is internal), the test verifies:

    1. Configure RBAC_ENABLED=true, RBAC_PDS_ENABLED=true.
    2. Mock `rbacService.hasPrivilege("gnarly", "SELECT", "PDS", anyString())` to return false.
    3. Construct the CatalogImpl via `newCatalogImpl(versionContextResolver)`.
    4. Call `catalog.getTable("nonexistent-id")`.
    5. Assert the result is null (dataset not found = no enforcement needed = null).
    6. Verify `rbacService.hasPrivilege` was `never()` called (null table bypasses RBAC -- the
       null guard fires before isRbacDeniedForPds).
    7. Add a comment: "Full behavioral path tested in TestRbacIntegration; this unit test
       documents that getTable(String) now wires through isRbacDeniedForPds."

    Follow the existing test pattern at lines 2144-2192: same config setup, same mock style,
    `newCatalogImpl(versionContextResolver)` constructor.

    **Test 2 -- NEW FILE TestDropViewHandler.java: DROP VIEW with DROP-only user gets Permission denied:**

    Create a new file `TestDropViewHandler.java` in the same package as `TestDescribeTableHandler.java`
    (`sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/`). Follow the
    `TestDescribeTableHandler` pattern exactly for mock setup (lines 200-226 from research).

    Class structure:
    - JUnit 4 (`@RunWith(MockitoJUnitRunner.class)`) to match TestDescribeTableHandler convention.
    - `@Mock` fields: `SqlHandlerConfig config`, `QueryContext queryContext`, `Catalog catalog`,
      `UserSession session`.
    - `@Before setUp()`: wire `when(config.getContext()).thenReturn(queryContext)`,
      `when(queryContext.getCatalog()).thenReturn(catalog)`,
      `when(queryContext.getSession()).thenReturn(session)`.
    - Field: `DropViewHandler handler` initialized in setUp as `new DropViewHandler(config)`.

    Test method `testDropView_dropOnlyUser_getsPermissionDenied`:

    1. Create `NamespaceKey path = new NamespaceKey(List.of("myspace", "myview"))`.
    2. Mock `catalog.resolveSingle(any())` to return `path`.
    3. Mock `session.getSessionVersionForSource("myspace")` to return `VersionContext.NOT_SPECIFIED`.
    4. Leave `catalog.validatePrivilege(path, SqlGrant.Privilege.DROP)` as default (no exception = passes).
    5. Use `doThrow(UserException.validationError().message("Permission denied: SELECT privilege required on '%s'", path).buildSilently()).when(catalog).validatePrivilege(path, SqlGrant.Privilege.SELECT)`.
    6. Construct `SqlDropView` node -- check the constructor signature in the source code. It takes
       `SqlParserPos`, `SqlIdentifier` (compound path), boolean (shouldErrorIfViewDoesNotExist),
       and potentially version parameters. Use `SqlParserPos.ZERO`, compound identifier from the
       path list, `true` for shouldErrorIfViewDoesNotExist, `null` for optional version params.
    7. Call `assertThatThrownBy(() -> handler.toResult("DROP VIEW myspace.myview", node))`.
    8. Assert `.isInstanceOf(UserException.class)`, `.hasMessageContaining("Permission denied")`,
       `.hasMessageContaining("SELECT")`.
    9. Verify `catalog.getTableNoColumnCount(path)` was `never()` called (SELECT check prevents
       reaching table resolution).

    Required imports: `Catalog`, `DropViewHandler`, `SqlDropView`, `SqlGrant`, `NamespaceKey`,
    `VersionContext`, `UserSession`, `UserException`, `SqlHandlerConfig`, `QueryContext`,
    `SqlParserPos`, `SqlIdentifier`, Mockito statics.

    **Test 3 -- TestCatalogServiceHelper: createSource non-admin gets Permission denied:**

    In the existing `TestCatalogServiceHelper.java`, add a test method
    `testCreateSource_nonAdmin_getsPermissionDenied` using the existing `rbacEnabledHelper`
    fixture (lines 239-256 in research). The rbacEnabledHelper already has:
    - `rbacService = mock(RbacService.class)` with RBAC_ENABLED=true
    - `securityContext` principal returning "user"

    The test:
    1. Mock `when(rbacService.isAdminMember("user")).thenReturn(false)`.
    2. Create a `Source` object with `setName("newsource")` and `setType("NAS")`.
    3. Call `assertThatThrownBy(() -> rbacEnabledHelper.createCatalogItem(source))` -- check the
       exact method signature available on CatalogServiceHelper; it may be `createCatalogItem`
       or may route through a different public method. The research shows the private
       `createSource()` is called from `createCatalogItem()`. Use the public API.
    4. Assert `.isInstanceOf(UserException.class)`, `.hasMessageContaining("Permission denied")`,
       `.hasMessageContaining("administrators")`.
    5. Verify `sourceService` was `never()` called with `createSource(any(), any(), any())` --
       confirms the admin guard fires before any metadata leak.

    Check how existing tests in TestCatalogServiceHelper construct Source objects and call
    createCatalogItem. The CatalogEntity hierarchy and dispatch method may vary -- read the test
    file first to confirm the correct invocation path.
  </action>
  <verify>
    1. `grep -rn "testGetTableByDatasetId_pdsEnforcement" sabot/kernel/src/test/java/com/dremio/exec/catalog/TestCatalogImpl.java` finds the new test.
    2. `ls sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/TestDropViewHandler.java` confirms the new file exists.
    3. `grep -rn "testDropView_dropOnlyUser_getsPermissionDenied" sabot/kernel/src/test/java/com/dremio/exec/planner/sql/handlers/direct/TestDropViewHandler.java` finds the test.
    4. `grep -rn "testCreateSource_nonAdmin_getsPermissionDenied" dac/backend/src/test/java/com/dremio/dac/service/TestCatalogServiceHelper.java` finds the new test.
    5. All three test files compile without errors.
  </verify>
  <done>
    Three unit tests added: (1) TestCatalogImpl documents getTable(String) PDS enforcement contract,
    (2) NEW TestDropViewHandler proves DROP-only user gets "Permission denied: SELECT" not "Unknown view",
    (3) TestCatalogServiceHelper proves non-admin gets "Permission denied" on createSource not
    "already exists". Each test documents the exact security contract of its corresponding fix.
  </done>
</task>

</tasks>

<verification>
1. Production code: All three gaps closed (INT-01, Finding 1, Finding 4).
2. Test code: Three unit tests document the new contracts -- one per gap.
3. No regressions: `isRbacDeniedForPds` is safe for VDS (ViewTable check), `validatePrivilege` is
   safe when RBAC disabled (internal guard), admin check is gated on RBAC_ENABLED.
4. Grep checks confirm all three fixes are wired correctly.
5. New TestDropViewHandler.java follows TestDescribeTableHandler pattern exactly.
</verification>

<success_criteria>
- getTable(String datasetId) has `isRbacDeniedForPds(table, table.getPath())` after resolution
- DropViewHandler.toResult() has `validatePrivilege(SELECT)` between DROP check and getTableNoColumnCount
- CatalogServiceHelper.createSource() has admin guard before sourceService.createSource()
- TestCatalogImpl has testGetTableByDatasetId_pdsEnforcement
- TestDropViewHandler.java exists with testDropView_dropOnlyUser_getsPermissionDenied
- TestCatalogServiceHelper has testCreateSource_nonAdmin_getsPermissionDenied
- No TODO comments remain for INT-01
</success_criteria>

<output>
After completion, create `.planning/phases/13-code-hardening/13-01-SUMMARY.md`
</output>
